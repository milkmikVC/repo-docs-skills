interface:
  display_name: "Repo Docs Router"
  short_description: "Route every repository conversation through project docs"
  default_prompt: "Use $repo-docs-router at the start of this repository conversation to load the relevant project documentation."

policy:
  allow_implicit_invocation: true
